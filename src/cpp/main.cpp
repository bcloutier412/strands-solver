#include <folly/json.h>
#include <fstream> 
#include <iostream> 

int main() {
  std::cout << "hello" << std::endl;
  // We want to open the runtime_data JSON File

  // Reset the possible solutions to board

  // Run DLX algorithm
}